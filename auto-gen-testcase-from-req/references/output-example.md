# 输出格式示例

前置条件、操作步骤和预期结果均从 `1.` 开始；表格内多项用 `<br>` 分隔，单项也保留 `1.`。

# 功能名称 - 功能测试用例

> 对应需求文档：《需求名称》V1.0

## 用例概要统计

| 统计项 | 数量 |
|---|---:|
| 总用例数 | 3 |
| 高优先级 | 3 |
| 中优先级 | 0 |
| 低优先级 | 0 |
| 覆盖需求模块数 | 1 |

## 需求缺失或假设

| 类型 | 内容 | 处理方式 |
|---|---|---|
| 需求缺失 | 未定义导出文件格式 | 不编造格式，只验证需求明确的字段和值 |

## 一、目标业务页面

公共预警展示、筛选联动和下钻直接写在本页面用例中，不单建通用章节。

> ⚠️ **数据准备清单（执行前必读）**
> 1. `{{DATA: 具有目标页面权限的账号}}` — 用于 TC_PAGE_001、TC_PAGE_003
> 2. `{{DATA: 具有目标页面和成本维护权限的账号}}` — 用于 TC_PAGE_002
> 3. `{{DATA: 存在异常记录的店铺 A}}` — 用于 TC_PAGE_001
> 4. `{{DATA: 异常数与店铺 A 不同的店铺 B}}` — 用于 TC_PAGE_001
> 5. `{{DATA: 补录后可恢复正常的异常记录}}` — 用于 TC_PAGE_002
> 6. `{{DATA: 可使目标记录恢复正常的采购成本}}` — 用于 TC_PAGE_002

| 用例编号 | 用例标题 | 前置条件 | 操作步骤 | 预期结果 | 优先级 | 测试方法 |
|---|---|---|---|---|---|---|
| TC_PAGE_001 | 异常条数随店铺筛选刷新并保留下钻条件 | 1. `{{DATA: 具有目标页面权限的账号}}` 已登录<br>2. 两个目标店铺均存在可查询记录 | 1. 导航到“目标业务页面”。<br>2. 在“店铺”筛选框选择 `{{DATA: 存在异常记录的店铺 A}}`，点击“查询”。📷<br>3. 记录“成本异常 n 条”的 n。<br>4. 将店铺改为 `{{DATA: 异常数与店铺 A 不同的店铺 B}}`，点击“查询”。<br>5. 点击“成本异常 n 条”，查看筛选值和明细条数。📷 | 1. 切换店铺后 n 按当前范围刷新<br>2. 下钻保留店铺 B<br>3. 明细条数与当前 n 一致 | 高 | 需求分析法+场景法 |
| TC_PAGE_002 | 补录成本后异常记录消失 | 1. `{{DATA: 具有目标页面和成本维护权限的账号}}` 已登录 | 1. 在“目标业务页面”定位 `{{DATA: 补录后可恢复正常的异常记录}}`。<br>2. 记录当前异常条数并打开成本维护页面。<br>3. 在“采购成本”输入框输入 `{{DATA: 可使目标记录恢复正常的采购成本}}`，点击“提交”。📷<br>4. 返回原页面并重新查询。 | 1. 目标记录不再出现在异常明细中<br>2. 异常条数相对提交前减少 1 | 高 | 场景法+需求分析法 |
| TC_PAGE_003 | 输入值最小值边界规则 | 1. `{{DATA: 具有目标页面权限的账号}}` 已登录<br>2. 需求规定目标输入值最小为 `0.01` | 1. 导航到“目标业务页面”。<br>2. 在目标输入框输入下限相邻无效值 `0.009`，点击“查询”。<br>3. 改为下限有效值 `0.01`，再次查询。📷 | 1. 输入 `0.009` 时显示低于下限的校验提示<br>2. 输入 `0.01` 时通过校验并执行查询 | 高 | 边界值分析法+需求分析法 |

> 对应需求文档章节：4.3 目标业务页面；4.4 预警展示
> 自动化可行性：可自动化。步骤、断言和依赖数据均明确。

## 占位符写法

```text
错误：选择 {{DATA: 多组异常数不同的真实条件}} 中的条件 A。
错误：新增独立步骤准备 {{DATA: 目标记录}}。
正确：在“店铺”筛选框选择 {{DATA: 存在异常记录的店铺 A}}。
正确：将“店铺”改为 {{DATA: 异常数与店铺 A 不同的店铺 B}}。
```
